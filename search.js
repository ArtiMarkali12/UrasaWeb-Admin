const fs = require(" fs\); function walk(dir) { fs.readdirSync(dir).forEach(f => { const p = dir + \/\ + f; if (fs.statSync(p).isDirectory()) walk(p); else if (p.endsWith(\.jsx\) || p.endsWith(\.js\)) { const c = fs.readFileSync(p, \utf8\); const m = c.match(/dimensions|custom.*width|custom.*height|custom.*thickness|sizeSelection/gi); if (m) { console.log(p); console.log(" Matches:\, m.join(" \)); } } }); } walk(\src\);
